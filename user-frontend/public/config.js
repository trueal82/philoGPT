// Dev-mode stub — in production, server.js overrides this with runtime config.
// Values here are ignored because the Vite env vars (VITE_API_BASE_URL) take precedence.
window.__APP_CONFIG__ = undefined;
